# openspec/config.yaml
# Auto-generated by sdd-init — 2026-07-06
schema: spec-driven

context: |
  # chatbot — AI Chat Application

  Tech stack: Next.js 16 (App Router, Turbopack, React Compiler),
    React 19, TypeScript 5.6 (strict), Tailwind CSS v4,
    Shadcn UI (Radix Maia), Drizzle ORM (PostgreSQL/Neon),
    Next-Auth v5, Redis, Vercel AI SDK 6, Biome 2.3 (via ultracite),
    Playwright 1.50 (E2E only)

  Architecture: App Router with route groups (auth, chat).
    Route handlers for API, server actions for mutations.
    DB layer via lib/db (schema, queries, migrations).
    Auth via Next-Auth with credentials/guest flows.
    AI layer via lib/ai (models, prompts, providers, tools).
    Artifact system via lib/artifacts + component composition.
    Editor subsystem via lib/editor (ProseMirror + CodeMirror).
    43 chat components, 22 UI primitives (shadcn).

  Testing: E2E only (Playwright). No unit or integration tests.
    Test command: `pnpm test`.

  Style: Biome/ultracite (space indent 2), no barrel files,
    no explicit any allowed, ESLint-style complexity rules relaxed.

  Persistence: Hybrid (openspec files + Engram observations).

rules:
  proposal:
    - Include rollback plan for risky changes
    - One intent per proposal
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Delta specs only — ADDED/MODIFIED/REMOVED/RENAMED sections
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - Reference existing component/file patterns
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
    - Include test references where applicable
  apply:
    - Follow existing code patterns
    - Preserve existing conventions (Biome/ultracite, TS strict)
    tdd: false
    test_command: "pnpm test"
  verify:
    test_command: "pnpm test"
    build_command: "pnpm build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: true
  runner: playwright
  layers:
    unit: false
    integration: false
    e2e: true
  coverage: false
  quality:
    linter: "pnpm check"
    type_checker: "pnpm build (tsc)"
    formatter: "pnpm fix"
